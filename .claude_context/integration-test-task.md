# Integration Test Task

`.claude\skills\integration-test-writer\SKILL.md` 파일 참고해서 `tests\integrations\conversation.integration.test.ts`에 `src\features\conversation\conversation.route.ts`에 대한 통합 테스트 코드 작성해주세요.

또한 `generateAIReponse()` 함수는 모킹해주세요.

basePath는 `/api/conversations` 입니다.
